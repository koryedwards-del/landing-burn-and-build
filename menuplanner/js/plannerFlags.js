/**
 * Preference learning is off for now.
 * Swapping away a food this week is situational — not "I dislike this" or
 * "I prefer whatever replaced it." Re-enable only when we can score explicitly.
 */
export const FOOD_PREFERENCE_LEARNING_ENABLED = false;
